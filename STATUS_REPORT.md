# DC House System Status Report
**Date:** 2026-06-21  
**Branch:** `claude/determined-cerf-isuqid`  
**Previous report:** PR #11 (2026-06-14)

---

## Summary

The DC House residential management system is **operational and stable**. Nine new commits landed on `main` since the last report, fixing a member star-badge bug, a courier-photo upload failure, an iOS Safari scrolling bug, a duplicate honorific in parcel notifications, and refining session/reload behavior on the guard interface. No open issues; no regressions found.

---

## Changes Since Last Report (2026-06-14)

### `main` — 9 new commits

| SHA | Description |
|-----|-------------|
| `a9ad974` | Update group row boundaries: Gen1 1–16, Gen2 17–25, Gen3 26–40 (tightens the previous 2–50 range). |
| `d11304a` | Fix column scroll: add `min-height:0` to the full flex chain so `column-body` scrolls correctly. |
| `29e7ffe` | guard.html: auto page reload every 30 min to pick up new deployments. |
| `dcdea69` | guard.html: remember PIN session for 8h so the new auto-reload doesn't force re-entry. |
| `58ae8ed` | Fix double "คุณ" in parcel notification — `name` already includes the honorific prefix. |
| `381f10b` | Compress courier photo before upload (max 1280px JPEG) — fixes second-upload failures. |
| `ce89110` | Fix iOS Safari scroll cutoff — use `100dvh` instead of `100vh`. |
| `7fa88b9` | Fix `logMember` action name mismatch + require >5 uses for star badge. |
| `5721fd2` | Revert `logMember` sheet-write change; star threshold set to >3 uses. |
| `437738d` | Set star threshold to >4 uses (final value). |

**Impact:**
- **Courier workflow:** Guards previously hit upload failures on a second photo submission for the same parcel; client-side JPEG compression (max 1280px) resolves this.
- **Parcel notifications:** LINE messages no longer show a duplicated "คุณคุณ[name]" — the prefix is now applied once.
- **Guard interface UX:** Auto-reload every 30 min keeps guards on the latest deployment without forcing PIN re-entry for 8 hours; iOS Safari's standalone-mode scroll cutoff is fixed; column scrolling inside the courier panel now works in the full flex chain.
- **Star badge logic churned mid-week** (>5 → revert → >3 → >4) before settling on **>4 uses** as the final threshold — this was a multi-commit back-and-forth on the same feature, now resolved.
- **Car generation boundaries tightened** from Gen3 26–50 to Gen3 26–40, reducing the advertised fleet capacity.

---

### New Branches Discovered

No new exploratory feature branches since the last report. The previously-noted `claude/dashboard-tv-layout` and `claude/world-cup-team-picker-Uh00j` branches remain unmerged with no open PRs — no action needed unless the owner wants to merge them.

---

### Open PRs

| PR | Title | State |
|----|-------|-------|
| #10 | System status report: 2026-06-07 | Open (previous report, can be closed) |
| #11 | System status report: 2026-06-14 | Open (previous report, can be closed) |

### Open Issues

None.

---

## Current System State

| Component | Status |
|-----------|--------|
| Hosting | Vercel (serverless) ✅ |
| Primary data store | Google Sheets ✅ |
| Car fleet | Gen 1 (rows 1–16), Gen 2 (17–25), Gen 3 (26–40) ✅ |
| Guard interface | Operational — PIN session persists 8h, auto-reloads every 30 min, courier photo upload compressed and reliable ✅ |
| Parcel/LINE notifications | Operational — honorific duplication fixed ✅ |
| Member star badge | Operational — threshold settled at >4 uses ✅ |
| iOS Safari layout | Fixed (100dvh) ✅ |
| Open bugs | None ✅ |

## Branch Sync

| Branch | HEAD | Notes |
|--------|------|-------|
| `main` | `437738d` | Latest stable |
| `claude/determined-cerf-isuqid` (this branch) | `437738d` | In sync with main |

No divergence. No pending work to merge.

---

_Generated by [Claude Code](https://claude.ai/code)_
